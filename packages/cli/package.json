{
  "name": "@lanelayer/cli",
  "version": "0.4.1",
  "description": "LaneLayer CLI - Build and run HTTP server containers for payments and intent execution",
  "bin": {
    "lane": "dist/cli.js"
  },
  "files": [
    "dist",
    "kv-service",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && npm run postbuild",
    "postbuild": "rimraf kv-service && cpy \"../kv-service/src/**\" kv-service/src --flat=false && cpy \"../kv-service/{Dockerfile,package.json,package-lock.json,tsconfig.json}\" kv-service",
    "prepublishOnly": "npm run build",
    "start": "node dist/cli.js"
  },
  "author": "LaneLayer",
  "license": "Unlicensed",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lanelayer/cli.git"
  },
  "homepage": "https://github.com/lanelayer/cli#readme",
  "bugs": {
    "url": "https://github.com/lanelayer/cli/issues"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "cpy-cli": "^6.0.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.0.0"
  }
}